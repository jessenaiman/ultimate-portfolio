---
interface Props {
  children: any;
}

const { children } = Astro.props;
---

<div class="hidden xl:flex flex-col items-end fixed top-32 right-8 z-50">
  <div class="bg-base-200 rounded-3xl p-3 shadow-2xl border-8 border-base-300 w-72 h-[600px] overflow-hidden flex flex-col">
    <div class="h-6 flex items-center justify-center mb-2">
      <div class="w-24 h-5 bg-base-300 rounded-full"></div>
    </div>
    <div class="flex-1 bg-base-100 rounded-xl overflow-auto">
      <slot />
    </div>
  </div>
  <div class="text-sm text-center mt-2 text-base-content/60">
    Mobile Simulator
  </div>
</div>
